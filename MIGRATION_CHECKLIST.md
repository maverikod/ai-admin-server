# Чеклист проверки готовности к переезду

**Дата проверки:** 2025-01-27  
**Проверяющий:** AI Assistant  
**Статус:** ❌ **НЕ ГОТОВО** - обнаружены критические проблемы

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (блокируют переезд)

### 1. Несоответствие версий пакетов

**Проблема:**
- В `requirements.txt` указано: `mcp-proxy-adapter>=6.9.104`
- В документе указано: `mcp-proxy-adapter>=6.9.102`
- **Нужно:** Проверить актуальность и обновить документ или requirements.txt

**Действие:**
- [ ] Проверить последнюю стабильную версию `mcp-proxy-adapter`
- [ ] Обновить документ или requirements.txt для соответствия

### 2. Отсутствие критических зависимостей в requirements.txt

**Проблема:**
В `requirements.txt` **ОТСУТСТВУЮТ** обязательные пакеты:
- ❌ `mcp_security_framework>=1.6.0` - **ОТСУТСТВУЕТ**
- ❌ `queuemgr>=1.0.13` - **ОТСУТСТВУЕТ**
- ✅ `werkzeug>=3.1.4` - присутствует косвенно через другие зависимости

**Действие:**
- [ ] Добавить `mcp_security_framework>=1.6.0` в requirements.txt
- [ ] Добавить `queuemgr>=1.0.13` в requirements.txt (или проверить, что включен в mcp-proxy-adapter)

### 3. Использование несуществующего API очереди

**КРИТИЧЕСКАЯ ПРОБЛЕМА:**
Согласно документу, в версии 6.9.102 **НЕ СУЩЕСТВУЕТ** модуль `mcp_proxy_adapter.queue`, но в коде его используют **10 команд**:

**Команды с неправильными импортами:**
1. `commands/vast_create_command.py` (строки 20-21)
2. `commands/vast_destroy_command.py` (строки 20-21)
3. `commands/ssh_exec_command.py` (строки 59-60)
4. `commands/ftp_upload_command.py` (строки 118-119)
5. `commands/ftp_mkdir_command.py` (строки 96-97)
6. `commands/ftp_rename_command.py` (строки 102-103)
7. `commands/ftp_info_command.py` (строки 96-97)
8. `commands/docker_pull_command.py` (строка 96) - использует `TaskQueueManager` (не существует)
9. `commands/queue_remove_task_command.py` (строка 16)
10. `commands/queue_cancel_task_command.py` (строка 16)

**Правильный API (согласно документу):**
```python
from mcp_proxy_adapter.integrations.queuemgr_integration import (
    QueueManagerIntegration,
    QueueJobBase,
    QueueJobStatus,
    get_global_queue_manager,
    init_global_queue_manager
)
```

**Действие:**
- [ ] Заменить все импорты `from mcp_proxy_adapter.queue import ...` на правильные
- [ ] Обновить использование API в этих 10 командах
- [ ] Проверить, что `docker_pull_command.py` использует правильный API

### 4. Использование собственного менеджера очереди

**Проблема:**
3 команды используют **собственный менеджер** вместо адаптера:
1. `commands/ftp_download_command.py` (строка 82)
2. `commands/ftp_list_command.py` (строка 66)
3. `commands/ftp_delete_command.py` (строка 70)

**Используют:**
```python
from ai_admin.task_queue.task_queue_manager import TaskQueueManager, Task, TaskType
```

**Действие:**
- [ ] Мигрировать эти 3 команды на новый API `queuemgr_integration`
- [ ] Или использовать `use_queue = True` для автоматической обработки

### 5. Неправильная инициализация очереди в DI

**Проблема:**
В `ai_admin/dependency_injection.py` (строка 131) используется:
```python
from ai_admin.queue.queue_manager import queue_manager, QueueManager
```

**Согласно документу:**
- Адаптер автоматически инициализирует очередь при `queue_manager.enabled: true` в конфиге
- НЕ нужно вручную регистрировать в DI

**Действие:**
- [ ] Удалить регистрацию очереди из `dependency_injection.py`
- [ ] Добавить секцию `queue_manager` в `config.json`
- [ ] Проверить, что адаптер автоматически инициализирует очередь

### 6. Отсутствие конфигурации очереди

**Проблема:**
В `config.json` **ОТСУТСТВУЕТ** секция `queue_manager` для автоматической инициализации.

**Нужно добавить:**
```json
{
  "queue_manager": {
    "enabled": true,
    "in_memory": true,
    "max_concurrent_jobs": 10,
    "completed_job_retention_seconds": 3600
  }
}
```

**Действие:**
- [ ] Добавить секцию `queue_manager` в `config.json`

## ⚠️ ВАЖНЫЕ ПРОБЛЕМЫ (требуют внимания)

### 7. Регистрация адаптеров безопасности

**Статус:**
- ✅ `SecurityManager` инициализируется в `ai_admin/security_integration.py`
- ❌ Адаптеры безопасности **НЕ регистрируются** через `SecurityManager.register_adapter()`
- ✅ Используется собственная система `UnifiedSecurityIntegration`

**Согласно документу:**
Для интеграции с `mcp_security_framework-1.6.0` нужно:
1. Создать адаптеры на основе `SecurityAdapter`
2. Зарегистрировать через `SecurityManager.register_adapter()`

**Действие:**
- [ ] Решить: использовать `mcp_security_framework` или оставить собственную систему
- [ ] Если использовать фреймворк - создать адаптеры на основе `SecurityAdapter`
- [ ] Зарегистрировать адаптеры в `SecurityManager`

### 8. Проверка использования `use_queue = True`

**Статус:**
Найдено только 2 использования `use_queue = True`:
- `commands/ftp_upload_command.py` (строки 68, 114)

**Согласно документу:**
Для простых команд рекомендуется использовать `use_queue = True` вместо ручного создания задач.

**Действие:**
- [ ] Проверить, можно ли заменить ручное создание задач на `use_queue = True` в других командах
- [ ] Обновить команды для использования автоматической очереди

### 9. Версия mcp_security_framework

**Проблема:**
- В документе указано: `mcp_security_framework-1.6.0`
- В requirements.txt: **ОТСУТСТВУЕТ**

**Действие:**
- [ ] Добавить `mcp_security_framework>=1.6.0` в requirements.txt
- [ ] Проверить, что все компоненты (SecurityAdapter, OperationContext, AuditLogger) доступны

## ✅ ЧТО УЖЕ УЧТЕНО В ДОКУМЕНТЕ

1. ✅ Детальный анализ изменений API очереди
2. ✅ Анализ использования безопасности
3. ✅ Примеры миграции команд
4. ✅ Паттерны использования очереди
5. ✅ План действий по этапам
6. ✅ Проверка доступности компонентов mcp_security_framework-1.6.0
7. ✅ Анализ конфигурации для локальной разработки

## 📋 ПЛАН ДЕЙСТВИЙ (приоритетный порядок)

### Этап 1: Критические исправления (1-2 дня)

1. **Обновить requirements.txt:**
   - [ ] Добавить `mcp_security_framework>=1.6.0`
   - [ ] Добавить `queuemgr>=1.0.13` (если не включен в адаптер)
   - [ ] Проверить версию `mcp-proxy-adapter` (6.9.102 или 6.9.104)

2. **Исправить импорты очереди:**
   - [ ] Заменить все `from mcp_proxy_adapter.queue import ...` на правильные импорты
   - [ ] Обновить 10 команд с неправильными импортами
   - [ ] Мигрировать 3 команды с собственным менеджером

3. **Обновить конфигурацию:**
   - [ ] Добавить секцию `queue_manager` в `config.json`
   - [ ] Удалить регистрацию очереди из `dependency_injection.py`

### Этап 2: Тестирование (1 день)

1. **Проверка импортов:**
   - [ ] Запустить скрипт проверки импортов
   - [ ] Убедиться, что все модули загружаются

2. **Проверка очереди:**
   - [ ] Проверить, что очередь инициализируется автоматически
   - [ ] Протестировать команды с очередью

3. **Проверка безопасности:**
   - [ ] Проверить, что SecurityManager работает
   - [ ] Протестировать SSL команды

### Этап 3: Интеграция безопасности (опционально, 1-2 недели)

1. **Создание адаптеров:**
   - [ ] Создать адаптеры на основе `SecurityAdapter`
   - [ ] Зарегистрировать через `SecurityManager.register_adapter()`

2. **Миграция существующих адаптеров:**
   - [ ] Использовать `SecurityAdapterWrapper` для legacy-адаптеров
   - [ ] Постепенно мигрировать на новый интерфейс

## 🎯 ИТОГОВАЯ ОЦЕНКА

**Готовность к переезду:** ❌ **30%**

**Что готово:**
- ✅ Документация и анализ выполнены
- ✅ Понимание изменений API
- ✅ План действий составлен

**Что НЕ готово:**
- ❌ Критические зависимости отсутствуют в requirements.txt
- ❌ 10 команд используют несуществующий API
- ❌ 3 команды используют собственный менеджер
- ❌ Конфигурация очереди отсутствует
- ❌ Неправильная инициализация в DI

**Рекомендация:**
**НЕ ПЕРЕЕЗЖАТЬ** до исправления критических проблем из Этапа 1.

## 📝 ЗАМЕТКИ

1. **Версия адаптера:** Проверить, действительно ли в 6.9.102/6.9.104 отсутствует `mcp_proxy_adapter.queue`. Возможно, документ устарел или версия отличается.

2. **Автоматическая инициализация:** Согласно документу, адаптер автоматически инициализирует очередь. Нужно проверить это на практике.

3. **Обратная совместимость:** Возможно, старый API все еще работает для обратной совместимости. Нужно проверить реальное поведение.

4. **Тестирование:** После исправлений обязательно запустить полный набор тестов.

