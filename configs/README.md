<!--
Author: Vasiliy Zdanovskiy
email: vasilyvz@gmail.com
-->

# configs/

Per **LAYOUT-04**: non-secret sample configuration and templates.

This repository’s primary application examples live under **`config/`** at the repo root (`config.example.json`, `auth.example.json`). Use **`configs/`** for additional sample snippets, schema dumps, or environment-specific examples that should not hold secrets.
